{% extends 'admin/base_admin.html.twig' %}

{% block title %}SocialMedia{% endblock %}

{% block in_body %}
    <h1>SocialMedia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ social_media.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ social_media.name }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ social_media.lien }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ social_media.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin.social.index') }}">Retour a la liste</a>

    <a href="{{ path('admin.social.edit', {'id': social_media.id}) }}">edit</a>

    {{ include('admin/social_media/_delete_form.html.twig') }}
{% endblock %}
